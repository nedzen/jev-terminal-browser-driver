# Draft for a pull request to NousResearch/hermes-agent plugin-catalog/.
# `hermes plugins validate .` passes on the repository root.
# Replace sha with the commit that is actually published, then open the catalog PR.
name: jev-driver
repo: https://github.com/nedzen/jev-terminal-browser-driver
sha: "0000000000000000000000000000000000000000"
description: >
  Drive one visible terminal-browser pane with Jev. Clicks, types, and likes
  in the user's logged-in browser. The terminal-browser program must be on
  PATH. It is not a Hermes skill. Debug outlines and hidden-browser attach
  are off unless the user asks.
maintainer: nedzen
tier: community
category: web
docs_url: https://github.com/nedzen/jev-terminal-browser-driver#readme
version: "0.1.0"
platforms: [macos, linux]
capabilities:
  provides_tools:
    - jev_drive
  provides_hooks: []
  provides_middleware: []
  requires_env:
    - OPENROUTER_API_KEY
